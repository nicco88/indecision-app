.option {
  border-bottom: 1px solid color($light-blue shade(10%));
  display: flex;
  justify-content: space-between;
  padding: $l-size $m-size;

  &__text {
      color: #fff;
      font-weight: 500;
      font-size: 2rem;
      margin: 0;
      word-break: break-all;
  }
}